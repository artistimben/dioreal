; ── PHP Upload Limits for High-Resolution Images ──
; Bu dosya PHP-FPM (nginx/apache+php-fpm) kullanılan
; sunucularda .htaccess php_value direktiflerinin
; yerine geçer.

upload_max_filesize = 50M
post_max_size = 55M
memory_limit = 256M
max_execution_time = 300
max_input_time = 300
